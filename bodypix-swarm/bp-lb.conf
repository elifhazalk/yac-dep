http {
    upstream bodypix {
        server tasks.bodypix:3000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://bodypix;
        }
    }
}

events { worker_connections 1024; }
